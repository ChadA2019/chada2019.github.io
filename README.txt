BalanceIQ v9.9.2 — Form & Dialog UX Polish

This release makes Cancel reliable across transaction, receipt, merchant-rule, import-preview, import-choice and name-management dialogs. Cancel and Escape no longer trigger required-field validation. When a user has typed or changed something, BalanceIQ asks before discarding unsaved changes. Clicking the dialog backdrop also closes safely where supported.
